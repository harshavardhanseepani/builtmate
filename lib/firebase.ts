// BuildMate uses JWT / custom authentication and in-memory/Supabase data stores.
// Firebase SDK initialization is disabled to prevent build-time prerender errors.

export const auth: any = null;
export const db: any = null;

